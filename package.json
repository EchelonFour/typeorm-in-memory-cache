{
  "name": "typeorm-in-memory-cache",
  "version": "1.0.1",
  "description": "Provides an in-memory cache for typeorm",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepublish": "npm run build",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Frank Fenton",
  "license": "MIT",
  "devDependencies": {
    "typeorm": "^0.2.25",
    "typescript": "^3.9.3"
  },
  "dependencies": {
    "node-cache": "^5.1.0",
    "tslib": "^2.0.0"
  },
  "peerDependencies": {
    "typeorm": "*"
  }
}
